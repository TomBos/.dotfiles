#!/usr/bin/env bash

export NVM_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/nvm"

if [ -d "$NVM_DIR" ]; then
    [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
else
    echo "NVM directory not found at $NVM_DIR"
fi

