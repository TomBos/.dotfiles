" Basic Vim Configuration

" Turn off compatibility mode with vi
set nocompatible

" Enable line numbers
set number

" Use system clipboard (allows copy-paste between Vim and other programs)
set clipboard=unnamedplus

" Enable syntax highlighting
syntax enable

" Enable auto-indentation
set smartindent
set autoindent
set tabstop=4       " Set tab width to 4 spaces
set shiftwidth=4    " Set indentation width to 4 spaces
set expandtab       " Use spaces instead of tabs

" Enable line wrapping
set wrap

" Show matching parentheses/brackets
set showmatch

" Set search to be case insensitive
set ignorecase
set smartcase       " Case sensitive if uppercase letters are used in the search

" Enable line numbers
set number

" Highlight search results as you type
set incsearch

" Enable line wrapping for better readability
set wrap

" Show the cursor line
set cursorline

" Set the background to dark (change if you prefer light theme)
set background=dark

" Simple colorscheme
colorscheme desert

