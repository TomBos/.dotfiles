#!/usr/bin/env bash

if [[ "$1" == "-h" ]]; then
	echo "Use \`brightness [+/- <value>]\`, to modify screen brightness"
	echo "OR  \`brightness <value>\`, to set screen brightness"
	exit
fi

if [[ "$1" == *-* ]]; then
    value="${1//-/}-"
else
    value="$1"
fi

brightnessctl set "$value"
echo

